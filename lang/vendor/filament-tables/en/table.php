<?php

/**
 * Filament Tables translation overrides (English).
 *
 * Laravel merges this recursively over the package defaults, so only the keys
 * that differ need to be declared here.
 */
return [

    'filters' => [

        'actions' => [

            'reset' => [
                'label' => 'Clear filters',
            ],

        ],

    ],

];
